services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8000}:8000"
    environment:
      APP_ROOT: /app
      DB_PATH: ${DB_PATH:-/app/data/krisha.sqlite3}
      SQLITE_JOURNAL_MODE: ${SQLITE_JOURNAL_MODE:-WAL}
      PRICE_MODEL_ROUTING: ${PRICE_MODEL_ROUTING:-city_auto}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
      TELEGRAM_BOT_USERNAME: ${TELEGRAM_BOT_USERNAME:-}
      COOKIE_SECURE: ${COOKIE_SECURE:-}
      PREDICT_RATE_LIMIT_PER_MINUTE: ${PREDICT_RATE_LIMIT_PER_MINUTE:-10}
      PREDICT_RATE_LIMIT_PER_HOUR: ${PREDICT_RATE_LIMIT_PER_HOUR:-60}
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    profiles:
      - https
    depends_on:
      - app
    environment:
      APP_DOMAIN: ${APP_DOMAIN:-}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped

  refresh:
    build: .
    profiles:
      - tools
    environment:
      APP_ROOT: /app
      DB_PATH: ${DB_PATH:-/app/data/krisha.sqlite3}
      SQLITE_JOURNAL_MODE: ${SQLITE_JOURNAL_MODE:-WAL}
      PRICE_MODEL_ROUTING: ${PRICE_MODEL_ROUTING:-city_auto}
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    command:
      [
        "python",
        "scripts/refresh_listings.py",
        "--kind",
        "manual",
        "--pages",
        "1",
        "--max-listings",
        "3",
        "--min-delay",
        "0",
        "--max-delay",
        "0"
      ]

  telegram_bot:
    build: .
    profiles:
      - bot
    environment:
      APP_ROOT: /app
      APP_PUBLIC_URL: ${APP_PUBLIC_URL:-https://kvartiry-ai.kz}
      DB_PATH: ${DB_PATH:-/app/data/krisha.sqlite3}
      SQLITE_JOURNAL_MODE: ${SQLITE_JOURNAL_MODE:-WAL}
      PRICE_MODEL_ROUTING: ${PRICE_MODEL_ROUTING:-city_auto}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_DIGEST_HOUR_ASTANA: ${TELEGRAM_DIGEST_HOUR_ASTANA:-9}
      PREDICTION_CACHE_TTL_SECONDS: ${PREDICTION_CACHE_TTL_SECONDS:-21600}
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    command: ["python", "scripts/telegram_bot.py"]
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
